# Handbook Overview (L1)

> Auto-generated by `handbook_phase3.py` on 2026-07-21T03:53:18.068848+00:00
> Paper ref: arXiv:2607.13285v1, §3.2 Phase III

## System architecture

- **Language**: python
- **Leaf mode**: `function` (function-level decomposition)
- **Files scanned**: 2
- **Functions**: 12
- **Call edges**: 101
- **State accesses**: 5

## Execution model

The harness follows a 6-stage execution model:

| Stage | Description |
|-------|-------------|
| `init` | Initialization — Bootstrap session, state init, integrity check |
| `interpret` | Interpretation — Triage task, decide skills, load MCPs |
| `plan` | Planning — DDLP, TPSP, declare loop |
| `execute` | Execution — Worker, build, edit, commit |
| `verify` | Verification — fixed_check, smoke tests, screenshot |
| `handoff` | Handoff — Closeout, update PLAN-VIVO §8, handoff next agent |

## Stage relationships

```
init → interpret → plan → execute → verify → handoff
                                         ↓
                                       (loop until closeout)
```

## Global state flow

State registers tracked (Vista Z):
- See `registers.md` for full list

## How to navigate

1. Read `index.md` for the table of stages and leaves.
2. For each stage of interest, open `stages/<id>.md`.
3. For state-register mutations, read `registers.md`.
4. For verification of code sites, run `handbook_verify.py`.

## Maintenance

This handbook is auto-regenerated by `handbook_resync.py` after every commit.
